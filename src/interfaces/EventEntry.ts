export interface IEventEntry {
  raceNumber: number;
  name: string;
  kartName: string;
  kartShortName: string;
  category: string;
  unactive: boolean;
  numberOfGears: number;
  maxRPM: number;
}
